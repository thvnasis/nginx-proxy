events {}

http {
  resolver 127.0.0.11 valid=30s;

  server {
    listen 80;

    location / {
      proxy_pass http://landing-page-production.railway.internal;
      proxy_set_header Host $host;
    }

    location /rabit-delux-retreat {
      return 200 "route exists";
    }
  }
}
